type User {
  id: ID! @unique
  name: String!
  email: String!
}

type Recipe {
  id: ID! @unique
  title: String!
  description: String!
  image: String
  largeImage: String
  #createdAt: DateTime!
  #updatedAt: DateTime!
  # user: User!
}